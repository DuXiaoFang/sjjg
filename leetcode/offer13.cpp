#include<iostream>
#include<vector>
using namespace std;


class Solution{

private:
    int dfs(int i,int j,int si,int sj,int cou,vector<vector<bool>> dp,int m,int n,int k){
        if(i >= m || j >= n || k < si+sj || dp[i][j]) return 0;
        for(int ){
            cou += 1;
            

        }
    }
public:
    int movingCount(int m, int n, int k){
        vector<vector<bool>> dp(m,vector<bool>(n));
    }
};